#include <stdio.h>
#include <unistd.h>
// Script Temps pour le projet
int main()
{
            int Temps=60;

        while (Temps>0)
        {
            printf("temps: %d\n",Temps);
            Temps--;
            sleep(1);

        }

    printf("Temps ecoule.\n");
        return 0;

}
